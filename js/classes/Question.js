class Question {
    constructor(id, title, answers, correctAnswer) {
        this.id = id
        this.title = title
        this.answers = answers
        this.correctAnswer = correctAnswer
    }
}

export default Question